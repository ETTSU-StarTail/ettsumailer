# コミットメッセージ規約（Conventional Commits + Gitmoji）

## フォーマット

```
<type>(<scope>): <絵文字> <短い説明（50字以内）>

または

<type>: <絵文字> <短い説明（50字以内）>

- <変更の背景や理由・使用している技術や意図・将来の理解に役立つ情報（短文・箇条書き）>

<フッター（必要に応じて）>
```

**ルール：**

- メッセージは必ず**日本語**で記述する
- Conventional Commits 形式 + Gitmoji（絵文字）に準拠する
- 過去のコミット履歴のスタイルに関わらず、必ず上記フォーマットを使用する
- `scope` は**任意**（不要な場合は省略可）
- 絵文字は`<type>(<scope>):` または `<type>:` の直後、説明文の前に 1 個のみ使用する（複数絵文字は使用しない）
- 「短い説明（50字以内）」は、全角・半角を区別せず**50文字以内**を目安にする
- 本文の箇条書きは**省略可能**（必要な場合のみ記載）
- 破壊的変更は `!` またはフッターに `BREAKING CHANGE:` で明示する
- Issue や PR に対応する変更は、フッターに `Fixes #<番号>` または `Closes #<番号>` を記載する
- 複数 Issue を 1 行で記載してよい（例: `Fixes #123, #124`）
- 複数 Issue を確実に自動クローズしたい場合は、Issue ごとにキーワードを繰り返す記法を推奨する（例: `Fixes #123, fixes #124`）
- merge / revert など Git の自動生成コミットメッセージは本規約の**適用対象外**とする

---

## 破壊的変更（BREAKING CHANGE）

後方互換性を壊す変更を含む場合は、以下のいずれかの方法で明示する。

**方法 1: `!` 記法**（概要のみで伝わる場合）

```
<type>(<scope>)!: <絵文字> <概要メッセージ>
```

**方法 2: フッターに `BREAKING CHANGE:` を記述**（詳細な説明が必要な場合）

```
<type>(<scope>): <絵文字> <概要メッセージ>

<詳細メッセージ>

BREAKING CHANGE: <後方互換性が壊れる内容と移行方法>
```

---


## type と推奨絵文字

変更内容に合致する type を選び、対応する推奨絵文字を使用する（原則推奨。文脈に応じて例外を許可）。

> **補足：**
> - `.gitignore` の追加・更新は `chore` + 🙈（:see_no_evil:）を使います（gitmoji公式）。
> - その他の雑多な運用・メンテナンス作業は `chore` + 🧹（:broom:）を使います（慣習）。

| type | 用途 | 推奨絵文字 |
|------|------|-----------|
| feat | 新機能の追加（実装） | ✨ |
| fix | バグ修正（軽微は 🩹、重大なホットフィックスは 🚑️） | 🐛 |
| docs | ドキュメントのみの変更 | 📝 |
| style | コードの意味に影響しない変更（空白・フォーマット・セミコロンなど） | 🎨 |
| refactor | バグ修正でも機能追加でもないコード変更 | ♻️ |
| perf | パフォーマンス改善のためのコード変更 | ⚡️ |
| test | テストの追加または修正 | ✅ |
| build | ビルドシステム・外部依存関係への変更（設定→🔧 / 依存追加→➕ / 依存削除→➖ / アップグレード→⬆️ / ダウングレード→⬇️） | 🔧 |
| ci | CI 設定ファイルやスクリプトへの変更（追加・更新→👷 / 修正→💚） | 👷 |
| chore | 雑多な作業・運用系・メンテナンス | 🧹 / 🙈 |

---

## type 選定の優先順位（MUST）

Conventional Commits 公式では `feat` は「新機能追加」、`fix` は「バグ修正」を表す。`docs` などの追加 type も許容される。
本規約では誤判定を避けるため、以下の順序で **必ず** 判定する。

1. **変更ファイルの分類を先に行う**
	- 変更対象がドキュメントファイルのみ（例: `*.md`, `*.adoc`, `docs/**`）なら、内容が「追加」「修正」「削除」のいずれでも **`docs` を選ぶ**
	- この場合、`feat` / `fix` は選ばない
2. **ドキュメント以外の実コード変更があるかを確認する**
	- 実コードに新機能追加がある場合のみ `feat`
	- 実コードの不具合修正がある場合のみ `fix`
3. **`feat` と `fix` が同時に見える場合**
	- 可能な限りコミットを分割する（Conventional Commits FAQ 推奨）
	- 分割できない場合は、主目的（ユーザー影響が大きい変更）を type として採用し、本文で補足する
4. **破壊的変更がある場合**
	- type に関係なく `!` または `BREAKING CHANGE:` を付与する

> Rationale: 変更「内容（追加/修正）」ではなく、変更「対象（ドキュメント/コード）」を先に判定することで `docs` の誤分類を防ぐ。

---

## 絵文字選定のルール（MUST）

- 絵文字は type の直後に 1 個のみ置く
- `docs` の場合は原則 **📝 (`:memo:`)** を使用する（Gitmoji 公式: Add or update documentation）
- `.gitignore` の追加・更新は `chore` + **🙈** を優先する
- type 推奨絵文字と「追加の絵文字パターン」が競合する場合、まず type の整合性を優先し、そのうえで必要時のみ置き換える

---

## 判定チートシート（迷いやすいケース＋特殊ケース）

`docs` / `feat` / `fix` 以外も含め、誤判定しやすいケースを 1 つの表に統合する。

| ケース | 推奨 type / 絵文字 | 判定メモ |
|---|---|---|
| `README.md` に新しい章を追加 | `docs: 📝` | 追加でも対象がドキュメントのみなら `docs` |
| `docs/` 配下の誤字修正 | `docs: 📝` | 修正でも `fix` にはしない |
| `docs/` 更新 + 実装コードで機能追加 | 原則コミット分割（`docs` と `feat`） | 分割不可なら主目的で選定 |
| `docs/` 更新 + 実装コードのバグ修正 | 原則コミット分割（`docs` と `fix`） | 分割不可なら主目的で選定 |
| 依存関係の追加 | `build: ➕` | `package.json` / `Gemfile` など |
| 依存関係の削除 | `build: ➖` | 未使用依存の整理 |
| 依存関係の更新（アップグレード） | `build: ⬆️` | lockfile 更新のみでも可 |
| 依存関係の更新（ダウングレード） | `build: ⬇️` | 互換性対策で下げる場合 |
| 依存バージョン固定 | `build: 📌` | 範囲指定から固定版へ |
| CI 設定の追加・更新 | `ci: 👷` | `.github/workflows/**` など |
| CI 失敗修正 | `ci: 💚` | ビルドパイプライン復旧 |
| 設定ファイル更新（CI 以外） | `build: 🔧` | ツール/ビルド設定の変更 |
| テストのみの変更 | `test: ✅` | 実装コードに変更なし |
| 意図的に失敗するテスト追加 | `test: 🧪` | 再現テスト先行時 |
| フォーマットのみ変更 | `style: 🎨` | 挙動に影響しない整形 |
| ファイル移動・リネームのみ | 原則 `refactor: 🚚` | 対象が docs のみなら `docs: 📝` を優先 |
| ライセンスのみ更新 | 原則 `docs: 📄` | ライセンス文書の更新 |

> Rationale: 判定基準を 1 か所に集約し、生成時の揺れ（例: lockfile 更新が `chore` や `fix` に流れる）を防ぐ。

---

## 追加の絵文字パターン

type 推奨以外に変更の性質を補足したい場合の**置き換え候補**として使用する（先頭絵文字 1 個のみ）。

| 絵文字 | 用途 |
|-------|------|
| 🚀 | デプロイ |
| 🎉 | プロジェクト開始 |
| 🔖 | リリース・バージョンタグ |
| 🔥 | コードまたはファイルの削除 |
| ⚰️ | 使われていないコードの削除 |
| 🗑️ | 非推奨（削除予定）コードのマーク |
| 💥 | 破壊的変更の導入 |
| ⏪️ | 変更のリバート |
| 🔀 | ブランチのマージ |
| 🚧 | 作業中（WIP） |
| 🚚 | ファイル・パス・ルートの移動またはリネーム |
| 🏗️ | アーキテクチャの変更 |
| 🔒️ | セキュリティ・プライバシー問題の修正 |
| 🔐 | シークレットの追加・更新 |
| 🙈 | .gitignore の追加・更新（chore type で使用） |
| 🧹 | 雑多な作業・運用系・メンテナンス（chore type で使用） |
| 📄 | ライセンスの追加・更新 |
| 🍱 | アセットの追加・更新 |
| 💄 | UI・スタイルファイルの追加・更新 |
| 💡 | ソースコードへのコメント追加・更新 |
| 🔊 | ログの追加・更新 |
| 🔇 | ログの削除 |
| 🏷️ | 型の追加・更新 |
| 🦺 | バリデーション関連コードの追加・更新 |
| 🥅 | エラー捕捉処理の追加 |
| 👔 | ビジネスロジックの追加・更新 |
| 🛂 | 認可・ロール・権限に関するコードの変更 |
| 🧱 | インフラ関連の変更 |
| 🧑‍💻 | 開発者体験（DX）の改善 |
| 🚸 | UX・ユーザビリティの改善 |
| ♿️ | アクセシビリティの改善 |
| 🌐 | 国際化・ローカライズ |
| 🧵 | マルチスレッド・並行処理関連の変更 |
| 🩺 | ヘルスチェックの追加・更新 |
| 📌 | 依存関係を特定バージョンに固定 |
| 👽️ | 外部 API 変更への追従 |
| 📦️ | コンパイル済みファイル・パッケージの追加・更新 |
| 📈 | 分析・トラッキングコードの追加・更新 |
| 🚨 | コンパイラ・リンター警告の修正 |
| 🚩 | フィーチャーフラグの追加・更新・削除 |
| 🔨 | 開発スクリプトの追加・更新 |
| 💬 | テキスト・リテラルの追加・更新 |
| 🗃️ | データベース関連の変更 |
| 📱 | レスポンシブデザインへの対応 |
| 📸 | スナップショットの追加・更新 |
| 🔍️ | SEO の改善 |
| 🤡 | モックの追加・更新 |
| 🧪 | 失敗するテストの追加 |
| 🌱 | シードファイルの追加・更新 |
| 💫 | アニメーション・トランジションの追加・更新 |
| ⚗️ | 実験的変更 |
| 🦖 | 後方互換性の追加 |
| 🧐 | データの探索・検査 |
| ✈️ | オフラインサポートの改善 |
| 💸 | スポンサー・金銭関連インフラの追加 |
| 👥 | コントリビューターの追加・更新 |
| ✏️ | タイポの修正 |

---

## 記述例

```
feat(auth): ✨ Google OAuth2.0 ログインの追加

- ユーザーが Google アカウントでログインできるようにした
- トークン管理は既存の AuthService に統合
```

```
fix(api): 🐛 検索結果が空のとき例外が発生する問題を修正

- null チェック漏れにより NullPointerException が発生していた
- 空配列の場合は早期リターンするよう修正

Fixes #42
```

```
fix(api): 🐛 検索時の例外処理を改善

Fixes #123, #124
```

```
refactor(config)!: 💥 設定管理クラスを環境変数ベースに刷新

- ConfigManager クラスを廃止し、環境変数による管理に移行

BREAKING CHANGE: ConfigManager を直接参照している箇所は環境変数経由に変更が必要
```

---

## Issue 番号の選定ルール

Issue 番号の誤指定を防ぐため、以下の順序で確認する。

1. **同一リポジトリかどうか**を確認する
	- 同一リポジトリなら `#123`
	- 別リポジトリなら `owner/repository#123`
2. 対象 Issue の **タイトル・状態（Open/Closed）・担当範囲** が今回の変更と一致することを確認する
3. 複数 Issue を閉じる場合は、**同じ原因で同時に解決されたものだけ**を列挙する
4. 不安がある場合は、コミット前に PR 本文の Development 連携や Issue 画面で再確認する

> 注意: `#番号` はリンク化されるが、自動クローズの挙動はキーワード記法に依存するため、確実性が必要な場合は `Fixes #123, fixes #124` のように記載する。

---

## 参考リファレンス

- [gitmoji](https://gitmoji.dev/)
- [Conventional Commits（日本語）](https://www.conventionalcommits.org/ja/v1.0.0/)
